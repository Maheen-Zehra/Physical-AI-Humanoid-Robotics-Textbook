# Physical AI Book (Docusaurus + RAG)

Run locally:
1. Copy .env.example -> .env and fill Qdrant values.
2. Install node dependencies: npm install
3. Install python dependencies: pip install -r requirements.txt
4. Start backend: uvicorn api.chat.main:app --reload
5. Start frontend: npm start
